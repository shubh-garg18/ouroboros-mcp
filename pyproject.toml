[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ouroboros"
version = "0.1.0"

[tool.hatch.build.targets.wheel]
packages = ["src/ouroboros", "src/code_fixer"]