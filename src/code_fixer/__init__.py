"""Ouroboros application: composes the `ouroboros` core runtime into the
Orchestrator and its scoped role agents (Analyzer, Fixer, Verifier, Critic,
PR Author). The core must never import from here."""
